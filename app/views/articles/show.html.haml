.cont
  - if user_signed_in?
    - if @article.user_id == current_user.id
      = link_to 'Edit Article', edit_article_path(@article)
  .article-title
    = @article.title
  %p= @article.content.html_safe

  -#.author
  -#  - if !@article.user_id.nil?
  -#    Author:
  -#    - login = User.find(@article.user_id).name
  -#    = link_to "#{login}", user_path(:id => @article.user_id)

  .com-title
    .left Comments

    -#- if user_signed_in?
    -#  .right= link_to 'Add a Comment', new_article_comment_path(@article)

    -#%a.comment-show Add a Comment
    -#.comment-form{hidden: true}= render 'comments/form'

    %a.comment-show Add a Comment
  .comment-form= render 'comments/form'

  - if @article.comments.empty?
    .no-comments No comments.
  - else
    %ul.comment_list
      = render :partial => 'comments/comment', :collection => @article.comments



